// Platform-specific wrapper for react-native-maps (Native platforms)
import MapView, { Marker, PROVIDER_GOOGLE } from 'react-native-maps';

export { MapView, Marker, PROVIDER_GOOGLE };
export const isMapAvailable = true;
