// Platform-specific wrapper for react-native-maps (Web platform - stub)
import { View } from 'react-native';

// Export stub components for web
export const MapView = View;
export const Marker = View;
export const PROVIDER_GOOGLE = null;
export const isMapAvailable = false;
