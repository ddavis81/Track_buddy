// Platform-specific wrapper for expo-camera (Native platforms)
import { Camera } from 'expo-camera';
import { CameraView } from 'expo-camera';

export { Camera, CameraView };
export const isCameraAvailable = true;
